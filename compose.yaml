services:
  itemcosttracker:
    image: itemcosttracker
    build: .
    restart: unless-stopped
    ports:
      - "5010:8080"
    volumes:
      - ./data:/data
    environment:
      DATA_DIR: /data
      ADDR: :8080
