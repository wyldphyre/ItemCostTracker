services:
  itemcosttracker:
    image: itemcosttracker
    restart: unless-stopped
    ports:
      - "5010:8080"
    volumes:
      - ./data:/data
    environment:
      DATA_DIR: /data
      ADDR: :8080
      # "Today" for active items is the calendar date in this zone.
      TZ: Australia/Perth
